import { FormatPaymentPipe } from './format-payment.pipe';

describe('FormatPaymentPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatPaymentPipe();
    expect(pipe).toBeTruthy();
  });
});
