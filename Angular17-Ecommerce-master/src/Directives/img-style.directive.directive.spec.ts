import { ImgStyleDirectiveDirective } from './img-style.directive.directive';

describe('ImgStyleDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new ImgStyleDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
